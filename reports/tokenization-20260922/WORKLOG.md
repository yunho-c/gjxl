# GPU tokenization and DC scheduling experiment

Active objective: implement and optimize GPU-resident AC tokenization; explore independent CPU DC groups and DC-AC / CPU-GPU overlap; determine exact-output complete-call and batch value before retaining changes.

Baseline: main 7659cac, clean detached /Users/yunhocho/GitHub/gjxl-tokenization-base-20260922. Candidate: perf/gpu-tokenization-20260922, /Users/yunhocho/GitHub/gjxl-gpu-tokenization-20260922. Separate Release builds, Xcode.app, Apple M4 Pro. Original main dirty work preserved. No commits/pushes requested.

Initial work: extracted existing bounded serializer dispatcher; independent DC-group output slots and concurrent scratch accounting. GJXL_EXPERIMENT_DC_WORKERS=1/2/4/8 in experimental build supplies ablation. GPU prototype and overlap implementation/qualification remain outstanding.

Completion requires: exact tokens/populations and complete bytes; memory/CPU admission and failure tests; ordinary complete-call paired comparisons; B1/B4 comparisons; optimized GPU variants and overlap comparisons; recorded keep/reject decision with reproducible artifacts. Stage-only timing is insufficient.

GPU v1/v2: exact completed output hashes on Kodak e7 and Alpine24MP e1/e7/e8. Independent public-tokenizer oracle passed 1024 submissions / 4096 groups. First fresh-buffer prototype exposed ~11ms CPU wait for ~2.5ms actual GPU execution (24MP/e7); stage timestamps measured ~1.8ms metadata, ~0.05ms offsets, ~0.9ms emit. GPU buffer preparation before execution is the leading explanation; not a hardware saturation claim.

GPU v3: single aligned arena through existing purgeable, budget-aware backend cache; explicit CPU DC overlap between Begin and Finish; exclusive wall attribution subtracts DC interval from AC. Oracle expanded to 2560 submissions / 10240 groups with immediate arena reuse. Reuse reduced steady 24MP/e7 wait to ~3.1ms and AC wall to ~6.1ms (~2.8ms exposed with parallel DC overlap). Preliminary whole-call comparisons saved under gpu-v3-screen; not yet final qualification. Cold/fresh behavior remains separately measurable. Next: tight output allocation / memory impact, broader paired qualification, B1/B4 and resource/failure tests.

v4 compact output: metadata/count submission followed by exact output allocation/emission. All 2560/10240 oracle cases pass in both dense and compact layouts. For 48MP/e7 compact metadata+output used ~148MB vs ~1GB maximum-capacity arena, recovering reuse and reducing exposed AC time substantially.

v5 guarded compact output: one additional tiny GPU group-prefix kernel computes compact offsets. A speculative output buffer uses a backend capacity hint; the emit kernel checks whole-frame capacity before any write. Overflow waits, grows, and repeats emission from unchanged metadata. v5 passed 2560/10240 cases for dense, two-submit compact, adaptive compact, and forced one-token-capacity retries. Both GPU allocation/submission/completion failure paths and live-lease/cache-trim tests pass (13 injected failure/recovery cases). DC passed 20 exact gradient/weighted parallel, CPU-cap, finite-memory-plan, partial-launch/recovery cases.

v6 tuning: 64/128/256 threads, 1/2/4/8 histogram shards, and specialized host reduction screened with paired whole calls. No clear gain over 128 threads / one shard / existing reduction so far. Adaptive compact plus DC overlap hides GPU execution in the 48MP/e7 case (steady wait ~0.003ms); CPU metadata preparation and histogram reduction still remain. Source and frozen binaries are retained per screen directory.

CPU cleanup control: best-effort bounded parallel destruction of direct AC token buffers, under GJXL_EXPERIMENT_PARALLEL_TOKEN_RELEASE=1. It preserves status/publication on launch failure; ordinary destruction handles remaining slots. Preliminary two-round screen: CPU-DC8 / CPU-DC8+cleanup / adaptive-GPU+overlap, respectively: 24MP e1 117.11/91.83/84.90ms; 24MP e7 387.19/376.35/350.02ms; 48MP e1 238.14/198.50/179.89ms; 48MP e7 943.56/888.89/863.40ms. These are screening data, not final intervals. This control is essential: GPU gain must also be compared to an improved CPU path, not only original main.

Full candidate build passed (281 incremental targets). Full 159-test suite now running, performance collection paused during tests. Original dirty main status rechecked unchanged. DJXL is available at /opt/homebrew/bin/djxl for independent decode validation.

Default full CTest passed 159/159 in 151.42s. Explicit combined-path tests then found two integration issues before qualification: batch idle bounds omitted the two new token pools, and the submission-count profiling assertion compared an adaptive cold reference to warmed capacity. Added separately planned token metadata/output idle capacities (active bytes remain in serializer working envelope), extended the shared Metal pool inventory, and primed the ordinary profiling reference consistently. Combined-path tests are being rerun. Performance screens before this admission fix remain diagnostic; final binary will include it.

Idle-bound fix rerun passed both combined resident-workflow and workflow-admission tests (7.33s / 17.18s). Array order preserves completed frame as the final pool; token metadata and token output have separate maxima so mixed-shape batches remain bounded. Default build is being refreshed after this internal-plan change.

One remaining kernel optimization worth screening before final freeze: DCT8 token emission has very short streams but currently spends a full SIMD group per transform/channel. A scalar-thread DCT8 emit variant can keep the coalesced SIMD metadata scan and reduce inactive emission lanes; it will be gated, oracle-tested, and compared against v6. Final qualification, B1/B4, decode validation and the decision report remain outstanding.

v7 scalar DCT8 emission: passed all four 2560-submission oracle layouts (dense, compact, adaptive, forced overflow) and the 13-failure matrix. A variant avoids the unused SIMD dispatch for pure DCT8 frames. In the two-round screen, scalar emission reduced 24MP/e1 GPU command duration from ~7.2ms to ~4.4ms and exposed AC time from ~6.8ms to ~4ms. Whole-call means SIMD/scalar: 24MP e1 83.34/81.09ms; 48MP e1 177.43/174.39ms. Mixed e7 showed no benefit (24MP 348.23/349.23ms, 48MP 860.79/863.00ms). Final experimental auto scalar flag value 2 therefore selects scalar only on pure DCT8 frames; value 1 preserves the mixed-frame ablation. One histogram shard and 128 threads remain the selected settings.

No more algorithm variants planned before qualification. Refreshing final build/tests, then freezing final capture and batch binaries. Final cohorts will include original main, DC parallelism, CPU cleanup control, and compact adaptive GPU + DC overlap + pure-DCT8 scalar emission. Qualification runners retain input/binary hashes, exact argv/env, source patch/new files, per-case bytes, raw timings, and resumable completion records. Updated runners reject resume with a changed runner hash.

Final verification: fixed the workflow-storage test to sum every non-completed-frame pool (two new token pools made the old literal four invalid). Combined experimental path passed 9/9 focused tests. Latest full default suite had one silent AC-strategy profiling/storage-test failure; 13 isolated candidate and 10 baseline repetitions passed. Retain as unresolved intermittent until final rerun; do not silently discard.

Unrestricted 48MP/e7 B4 pilot reached 44.0-44.2 GiB managed backing and substantial paging, with unstable multi-second timings. Final B1/B4 protocol changed to the same 24 GiB managed-memory allowance for all variants. Batch size is queue depth; resource planning may reduce in-flight work. Harness records the resulting plan. Unrestricted pilot is diagnostic only.

The 24 GiB pilot could not admit the 48MP work plan. Selected common budget is 36 GiB; capped pilot passes exact bytes with planned in-flight counts 2 at 24MP/e7 and 1 at 48MP/e7, both variants. The 48MP plan trims caches per image. Final pipeline launched with full default CTest, 360 profile processes, then 108 bounded batch processes. No benchmark runs concurrently with builds/tests.

Final full suite: 158/159; AC-strategy passed, but AQ profile kernel-name assertion failed intermittently. Reproduced the same final-CfL attribution failure on untouched baseline (1/24 concurrent stress runs), so it is not unique to this prototype. Temporary candidate diagnostics recorded registry lookup misses after thousands of evictions from the existing 512-slot raw-pipeline-pointer registry; tests create many backends. Diagnostic source patch/logs retained, instrumentation reverted. The fixed registry does not remove dead entries and can evict live names; this explains the observed missing dispatch names. No numerical-output failure was observed. Measurement remains valid only with nonempty kernel names verified in each retained capture; ordinary timing does not depend on names. No more broad reruns planned.

v7 final single-image matrix completed: 360 processes, all bytes exact. Batch 24MP/e1 first round showed ~10% regression against CPU+ at equal in-flight 4, despite a single-image gain. Preserving the frozen v7 matrix, starting one further bounded pure-DCT8 experiment: one threadgroup per AC group, shared packed nonzero/last metadata, parallel contiguous-slice prefix, atomic physical group allocation, scalar per-channel emit. Group-indexed CPU views preserve logical output order independently of physical allocation order. Guarded retry clears populations and recomputes the fused kernel. No compilation/testing concurrent with the final v7 batch measurement.

Group-fused kernel passes six full oracle runs at widths 64/128/256 with normal and forced-one-token capacities, plus the failure suite. Initial gate was placed before strategy discovery; the oracle caught misuse on DCT16 and the gate was moved after discovery before any performance measurements. At 24MP/e1, GPU command duration falls 4.4ms -> 2.3ms (128) / 2.1ms (256); two-round whole-call means ~77.34ms -> 76.35/76.16ms. At 48MP/e1, ~171.00ms -> 167.82/165.81ms. B4 24MP/e1 still loses to CPU+ despite the kernel gain. Starting independent holdout qualification at group width256, separate from normal mixed-frame width128; no universal B4 recommendation.

Final outcome: full latest build passed 159/159, plus 9/9 explicit combined/group-fused tests. V7 qualification completed 360 profile processes and 108 batch processes, 18 independent djxl decodes, exact bytes throughout, zero unnamed dispatches and zero batch pageout/swapout increases. Group-fusion follow-up completed 90 profile and 54 batch processes, all outputs matching independently decoded V7, zero unnamed dispatches/pageouts/swapouts. Fusion improved paired single latency 1.7%/3.4% at 24/48MP/e1; 12MP inconclusive. It does not fix 24MP/e1 B4 (fused throughput ~2.3% below V7 GPU and ~9% below CPU+). Retain GPU and fusion as workload-selectable latency paths; no blanket batch/default recommendation. No further algorithm changes planned; report/artifact final audit remains.

Final audit complete: both pipelines have complete terminal states; all report links and frozen executable hashes resolve; all 18 decoder results retained; saved-data main and follow-up analysis pass. Figure rendered and visually inspected. Original main status and clean baseline rechecked; no commits or remote operations. The report contains the keep decision and the repeatable batch regression.

## Commit checkpoint

Implementation committed as `59d976152937a5a2e171c87be83254569273a3c9` after confirming every changed tracked file and new source/test/tool file matched the frozen group-final qualification snapshot. Retained full CTest: 159/159; focused combined/fused tests: 9/9. No encoder code changed during checkpoint preparation. The report, summaries, provenance, and selected validation logs are committed separately; binaries and per-call artifacts remain local and are inventoried by SHA-256. The checkpoint preserves opt-in GPU behavior; a reversible default-on rollout with a CPU override is the recommended next policy experiment. No push or merge was performed.
